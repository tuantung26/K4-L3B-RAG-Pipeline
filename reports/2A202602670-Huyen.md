# Individual contribution report

Mỗi thành viên copy template này thành:

```text
reports/<student-id>-<short-name>.md
```

Giới hạn khuyến nghị: 1 trang, không chép lại README hoặc mô tả lý thuyết chung. Báo cáo không phải một bài pipeline cá nhân; mục đích là ghi nhận ownership và bằng chứng đóng góp trong sản phẩm nhóm.

---

## Thông tin

- Họ và tên: Đào Thị Huyền
- Mã học viên: 2A202602670
- Nhóm: IpadKids
- Repository/branch: huyenlili

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Chatbot UI | Xây dựng giao diện Streamlit Knowledge Desk: chat history, nhập câu hỏi, chọn số nguồn, trạng thái provider và xóa hội thoại. | [app.py](../app.py), commit `5e19a3a` (`Add Ui`) | Done |
| Citation/source display | Hiển thị danh sách nguồn, metadata, retrieval score, URL và excerpt cho từng câu trả lời. | [app.py](../app.py), commit `5e19a3a` | Done |
| Backend error handling | Kết nối `generate_with_citation`; khi pipeline/API key chưa sẵn sàng, UI hiển thị thông báo thay vì làm ứng dụng crash. | [app.py](../app.py), commit `5e19a3a` | Done |

Chỉ kê khai công việc có thể đối chiếu bằng file, commit, pull request, test hoặc kết quả evaluation.

## Quyết định kỹ thuật quan trọng

Mô tả tối đa hai quyết định mà bạn trực tiếp tham gia:

1. **Quyết định:** Dùng Streamlit session state để lưu lịch sử hội thoại và sources của từng câu trả lời.  
   **Lý do/evidence:** Cách này phù hợp với ứng dụng Streamlit hiện có, giữ được lịch sử khi app rerun và cho phép render lại citation cùng assistant message.  
   **Trade-off:** Lịch sử chỉ tồn tại trong session hiện tại, chưa có lưu trữ theo tài khoản hoặc database.

2. **Quyết định:** Hiển thị nguồn trong expander kèm title, source, score, URL và excerpt.  
   **Lý do/evidence:** Bám theo yêu cầu chatbot phải hiển thị citation/source và giúp người dùng kiểm tra evidence ngay dưới câu trả lời.  
   **Trade-off:** Giao diện phụ thuộc vào metadata đầy đủ từ retrieval; URL có thể không xuất hiện nếu document không có URL.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng: `python -m py_compile app.py`; `streamlit run app.py --server.headless true --server.port 8502`; gọi `http://localhost:8502` bằng `Invoke-WebRequest`; `pytest -q`.
- Kết quả trước/sau nếu có: `app.py` compile thành công; Streamlit khởi động và trả HTTP `200`. Test suite hiện có `17 passed, 3 failed`.
- Lỗi đã phát hiện và cách xử lý: UI bắt exception từ `generate_with_citation` và hiển thị trạng thái pipeline chưa sẵn sàng. Ba test còn fail do golden dataset/evaluation report thiếu hoặc rỗng và BM25 không trả kết quả; đây là phần pipeline/evaluation ngoài phạm vi UI.

## Điều còn hạn chế

- Một hạn chế cụ thể của phần tôi làm: UI đã gọi được API generation nhưng end-to-end answer còn phụ thuộc vào các module retrieval/generation và dữ liệu index của nhóm.
- Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện: bổ sung test UI tự động cho các trạng thái empty, backend error và hiển thị sources; sau đó tích hợp highlight citation trong nội dung trả lời.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 25/09/2026
- Tên thành viên: Đào Thị Huyền  
